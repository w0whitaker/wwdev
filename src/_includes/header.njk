{% set currentUrl = page.url %}

<header>
  <div>
  {% if currentUrl === '/' %}
    {% include 'logo.njk' %}
    <h1>{{ meta.siteName }}</h1>
    <h2 >{{ meta.siteDescription }}</h2>
    {% include 'logo.njk' %}
  {% endif %}
  </div>
</header>