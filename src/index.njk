---
layout: page.njk
title: home
pagination:
  data: collections.posts
  size: 2
  alias: posts
---
<h2>posts</h2>
<ul>
  {% for post in posts %}
  <li>
    <a href="{{ post.url }}">{{ post.data.title }}</a>
    <p>{{ post.data.excerpt }}</p>
  </li>
  {% endfor %}
</ul>